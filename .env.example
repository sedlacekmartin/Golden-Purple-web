# Google PageSpeed API klíč pro /scan/ nástroj
# Získáš zdarma v Google Cloud Console → APIs & Services → Credentials
# Na Vercelu nastav tuto proměnnou v: Project → Settings → Environment Variables
PAGESPEED_API_KEY=
